<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MinimalDex" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/background_surface</item>
        <item name="colorPrimaryVariant">@color/background_surface</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/background_surface</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/status_bar_dark</item>
        <item name="android:elevation">0dp</item>
        <!-- Customize your theme here. -->
        <item name="android:windowBackground">@color/background_surface</item>
        <item name="snackbarStyle">@style/loading_bar</item>
    </style>

    <style name="Theme.MinimalDex.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
</resources>